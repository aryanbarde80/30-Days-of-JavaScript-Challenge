// Example ->> console.log((100==10*10)? `True` : `False`)

// Define a number to check
let number = 10; // You can change this number to test different values

// Use the ternary operator to check if the number is positive
let result = (number > 0) ? 'The number is positive' : 'The number is not positive';

// Log the result to the console
console.log(result);